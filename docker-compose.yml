version: '3.8'

services:
  my_redis_cache:
    image: redis:latest
    container_name: my_redis_cache
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=redisAPIproject123
    command: ["redis-server", "--requirepass", "redisAPIproject123"]
